<!-- googleMapAPI key -->
<!-- AIzaSyDk2OCKu47cMQsQdXJCSveDNFU4fb1EMjk -->

<script src="//maps.google.com/maps/api/js?key=AIzaSyDk2OCKu47cMQsQdXJCSveDNFU4fb1EMjk"></script>
<script src="//cdn.rawgit.com/mahnunchik/markerclustererplus/master/dist/markerclusterer.min.js"></script>
<script src='//cdn.rawgit.com/printercu/google-maps-utility-library-v3-read-only/master/infobox/src/infobox_packed.js' type='text/javascript'></script> <!-- only if you need custom infoboxes -->

<div class="area_search">
  <form>
    <input type="text" id="search_address">
    <input type="button" value="地図検索" id="search_button">
  </form>
</div>

<div style='width: 800px; margin: 0 auto;'>
  <div id="map" style='width: 800px; height: 400px;'></div>
</div>

<script type="text/javascript">
    handler = Gmaps.build('Google');
    handler.buildMap(
      {
        provider: {
          zoom: 16,
          center: new google.maps.LatLng(34.6945, 135.195)
        },
        internal: {id: 'map'}
      },
      function(){
        markers = handler.addMarkers(<%=raw @hash.to_json %>);
        handler.bounds.extendWith(markers);
        // handler.fitMapToBounds();
      }
    );

    // 検索ボックスに入力された地域に移動
    $('#search_button').on('click', function(){
      var search_address = $("#search_address").val()
      var geocoder = new google.maps.Geocoder();
      geocoder.geocode(
        {
          "address": search_address,
          "region": "jp"
        },
        function(results,status){
          if(status==google.maps.GeocoderStatus.OK){
            // console.log(results[0].geometry.location.lat());
            handler.buildMap(
              {
                provider: {
                  zoom: 16,
                  center: new google.maps.LatLng(results[0].geometry.location.lat(), results[0].geometry.location.lng())
                },
                internal: {id: 'map'}
              },
              function(){
                markers = handler.addMarkers(<%=raw @hash.to_json %>);
                handler.bounds.extendWith(markers);
                // handler.fitMapToBounds();
              }
            );
          }
        }
      );
    });

    var setLatLng = function(event){
      $('#submit').attr('value', 3);
      $('#js_latitude').attr('value', event.latLng.lat());
      $('#js_longitude').attr('value', event.latLng.lng());
    }

    <% if user_signed_in? %>
      // クリックした地点の座標にマーカーを設置
      // google.maps.event.addListener(handler.getMap(), 'click', function(event){
      handler.getMap().addListener('click', function(event){
        <% infowindow = render "tweet", locals: { tweet: @tweet } %>
        var click_marker = handler.addMarkers([
          {
            "lat": event.latLng.lat(),
            "lng": event.latLng.lng(),
            "infowindow": <%=raw infowindow.to_json %>
          }
        ]);
        handler.bounds.extendWith(click_marker);
        //handler.fitMapToBounds();

        console.log(click_marker);
        google.maps.event.trigger(click_marker[0].getServiceObject(), 'click');
        console.log($('#submit'));
        $('#submit').attr('value', 3);
        $('#js_latitude').attr('value', event.latLng.lat());
        $('#js_longitude').attr('value', event.latLng.lng());
      });

      // infowindowが表示された時に発生するイベント
      // google.maps.event.addListener(infowindow, 'domready', function(){
      // handler.getMap().infowindow.addListener('click', function(){
      //   console.log("marker click");
      // });

      // markerがクリックされたときにそのマーカーの座標を出力
      // google.maps.event.addListener(infowindow, 'domready', function(){
      // handler.getMap().infowindow.addListener('click', function(){
      //   console.log("marker click");
      // });s

      //(1)マーカーをjQueryで取得して，triggerでclickする
      //(2)マーカーにaddListenerでclickされた場合のイベントを追加
      // marker.addListener('click', function(){
      // google.maps.event.addListener(click_marker[0].google_object, 'click', function(){
      //   console.log($('#submit'));
      //   console.log($('#js_latitude'));
      //   $('#js_latitude').attr('value', event.latLng.lat());
      //   $('#js_longitude').attr('value', event.latLng.lng());
      // });
    <% end %>

</script>
